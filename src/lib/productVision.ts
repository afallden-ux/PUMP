/**
 * ClimbCompare north star — keep every feature aligned with this.
 *
 * Users pick another climber and see two profiles side by side: height, weight,
 * max hang, max pull-up, hardest grade, assessments, weekly stats, and more.
 * Logging and analytics exist to make that comparison meaningful.
 */
export const PRODUCT_NORTH_STAR =
  "Side-by-side climber comparison — see how you differ on every metric that matters.";

export const COMPARE_ROUTE = "/compare";
